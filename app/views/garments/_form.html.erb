<%= simple_form_for @garment do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, placeholder: 'Nom', label:false %>

  <%= f.input :photo, as: :attachinary %>
  <%= f.button :submit, 'Scanner!', class: "btn btn-karl" %>

<% end %>
